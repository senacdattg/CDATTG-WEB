# ===========================================
# CDATTG - Docker local (pruebas en máquina de desarrollo)
# Copiar a .env:  cp .env.local.example .env
# Levantar:       make docker-local-up
# ===========================================

DB_USER=cdattg
DB_PASSWORD=cdattg_local_dev
DB_NAME=cdattg_web
DB_PORT=5433
DB_SSLMODE=disable
DB_TIMEZONE=America/Bogota

JWT_SECRET=dev-jwt-secret-cambiar-en-produccion-min-32-chars
JWT_EXPIRATION_HOURS=24

# Impresora de carnets (vacío = API de impresora apagada)
CARNET_IMPRESORA_API_KEY=

# Frontend en :9080; con nginx.local.conf la API va por /api (mismo origen)
CORS_ALLOWED_ORIGINS=http://localhost:9080,http://127.0.0.1:9080
VITE_API_BASE_URL=/api

SMTP_ENABLED=false

NEGOCIO_RELAXAR_RESTRICCION_ASISTENCIA=false
NEGOCIO_RELAXAR_COLISION_HORARIO_INSTRUCTOR=false

# SonarQube (análisis local; pegar token en .env, no versionar)
SONAR_HOST_URL=https://sonarqube.dataguaviare.com.co
SONAR_PROJECT_KEY=cdattg-go
SONAR_TOKEN=
