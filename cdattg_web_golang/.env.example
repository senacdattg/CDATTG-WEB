# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=jhon
DB_PASSWORD=1234
DB_NAME=cdattg_web
DB_SSLMODE=disable
DB_TIMEZONE=America/Bogota

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# JWT Configuration
JWT_SECRET=cdattg-web-golang-secret-key-change-in-production-minimum-256-bits
JWT_EXPIRATION_HOURS=24

# Impresora de carnets (vacío = API de impresora apagada)
CARNET_IMPRESORA_API_KEY=

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_ALLOWED_HEADERS=*
CORS_ALLOW_CREDENTIALS=true

# SMTP (correo para alertas a coordinadores)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply.local@sena
SMTP_ENABLED=false

# Alertas: fichas que no han iniciado toma de asistencia (correo a coordinadores)
ALERTAS_MINUTOS_DESPUES_INICIO_JORNADA=90
ALERTAS_ASISTENCIA_ENABLED=true

# SofiaPlus (módulo Complementarios: verificación pública de aspirantes)
SOFIA_PUBLIC_URL=http://senasofiaplus.edu.co/sofia-public/inscripcion/consultarinscripciones/buscadorInscripcionesUsuario.faces
SOFIA_ENC_KEY=
SOFIA_HEADLESS=true
SOFIA_TIMEOUT_SEGUNDOS=60
SOFIA_DIAGNOSTICO=true
SOFIA_SCRAPER_URL=http://sofia-scraper:8090

# Environment
ENV=development
